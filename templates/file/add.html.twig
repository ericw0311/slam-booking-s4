{% extends 'base.connected.html.twig' %}
 
{% block body %}

{{ form_start(form, {'action': path('file_add'), 'method': 'POST', 'class': 'form-horizontal'}) }}

<fieldset>
	<legend>Inscription</legend>
	{{ form_row(form.name) }}
	{{ form_rest(form) }}
	<button type="submit" class="btn btn-success"><i class="glyphicon glyphicon-ok"></i> Valider</button>
</fieldset>

{{ form_end(form) }}

{% endblock %}

{% block javascripts %}

document.getElementById("user_lastName").focus();

{% endblock %}
